# القنصل — نسخة Hostinger

## 1) البيانات الأساسية
عدّل `assets/js/config.js` وضع رقم واتساب الحقيقي، روابط Facebook/TikTok، الهاتف، البريد وعنوان Google Maps.

## 2) الفيديوهات والصور
- كل صفحة لها فيديو Hero مستقل.
- الوجهات والفنادق بها أماكن فيديو واضحة.
- الصور الحالية SVG تجريبية ويمكن استبدالها بصور حقيقية مرخصة.
- ضع ملفات MP4 حسب `assets/video/VIDEO_NAMES.txt`.

## 3) الموسيقى
ضع `qonsul-theme.mp3` داخل `assets/audio/`. زر الطائرة يشغّل ويوقف الموسيقى. التشغيل بالصوت عند فتح الصفحة غير مضمون في المتصفحات، لذلك يبدأ بعد تفاعل المستخدم.

## 4) Google Maps
ضع عنوان المكتب الحقيقي في `mapQuery`، وضع رابط Google Maps الحقيقي في `mapLink`.

## 5) لوحة التحكم
افتح `/admin/`. كلمة المرور المؤقتة: `ChangeMeNow123!` ويجب تغييرها قبل النشر.
اللوحة تعدل `data/flights.json` و`data/hotels.json` بدون MySQL.

## 6) SEO
النسخة تحتوي على title/description/canonical/OG/robots/sitemap. بعد النشر اربط Search Console وأرسل sitemap. لا يوجد ضمان لترتيب عالمي؛ الترتيب يعتمد أيضاً على المحتوى الحقيقي، السرعة، الروابط والثقة المحلية.

## 7) مهم
بيانات الأسعار والمواعيد الحالية تجريبية placeholders ولا يجب عرضها كبيانات حقيقية قبل إدخال بيانات العميل.
